#include<stdio.h>
void main(){
   int sum=0,i,n,A[100];
   printf("Enter the number");
   scanf("%d",&n);
   printf("Enter the array values");
   for(i=0;i<n;i+){
    sum+=A[i];
   }
   printf("Sum is %d",sum);

}